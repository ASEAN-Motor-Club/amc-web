<!-- Test stub replacing the real WebGL map for the (map) layout integration test.
     Explicit size so visibility assertions hold in the bare test root (the real map
     sizes itself through the app's full-height layout chain). -->
<div class="h-24 w-24" data-testid="map-stub">mock map</div>
