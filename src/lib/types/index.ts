// place files you want to import through the `$lib` alias in this folder.
export type Quaternion = {
  x: number;
  y: number;
  z: number;
  w: number;
};

export type Vector2 = {
  x: number;
  y: number;
};

export type Vector3 = {
  x: number;
  y: number;
  z: number;
};
